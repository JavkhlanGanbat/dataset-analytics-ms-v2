server:
  port: 8082

spring:
  application:
    name: analytics-service

services:
  dataset:
    base-url: ${DATASET_SERVICE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info
